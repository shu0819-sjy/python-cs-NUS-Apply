# Python 代码报错复盘
## 1. 字符串与整数拼接错误
- **错误代码**：`print("结果是" + 8)`
- **原因**：Python 不允许字符串与整数直接用 `+` 拼接
- **解决**：用逗号分隔 `print("结果是", 8)` 或转换类型 `print("结果是" + str(8))`

## 2. 输入未转类型错误
- **错误代码**：`num = input("输入数字")` → `num + 1`
- **原因**：`input()` 返回的是字符串，无法直接参与数学运算
- **解决**：用 `int()` 或 `float()` 转换类型 `num = int(input("输入数字"))`

## 3. 循环索引越界错误
- **错误代码**：`for i in range(0, n): nums[i] += nums[i-1]`
- **原因**：`i=0` 时 `nums[i-1]` 会指向数组最后一个元素
- **解决**：循环从 `i=1` 开始 `for i in range(1, n)`
